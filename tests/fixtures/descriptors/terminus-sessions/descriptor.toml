id = "known.terminus.sessions"
name = "Terminus session list"
priority = 50

[matches]
required_substrings = ["session\tstatus","shell\topen"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
