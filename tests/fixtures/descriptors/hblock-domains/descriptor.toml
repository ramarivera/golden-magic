id = "known.hblock.domains"
name = "hblock domain list"
priority = 50

[matches]
required_substrings = ["domain\tsource","ads.example\tlist"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
