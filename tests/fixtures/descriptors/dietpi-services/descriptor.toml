id = "known.dietpi.services"
name = "DietPi service list"
priority = 50

[matches]
required_substrings = ["service\tstate","ssh\tactive"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
