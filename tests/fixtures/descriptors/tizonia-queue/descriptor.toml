id = "known.tizonia.queue"
name = "tizonia queue list"
priority = 50

[matches]
required_substrings = ["track\tstatus","song\tplaying"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
