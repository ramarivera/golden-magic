id = "known.create-dmg.assets"
name = "create-dmg asset list"
priority = 50

[matches]
required_substrings = ["asset\tstate","app\tincluded"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
