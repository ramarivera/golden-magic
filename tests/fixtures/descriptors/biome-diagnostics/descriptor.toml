id = "known.biome.diagnostics"
name = "Biome diagnostic list"
priority = 50

[matches]
required_substrings = ["file\tseverity","src/main.ts\terror"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
