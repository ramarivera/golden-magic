id = "known.claude-hud.panels"
name = "claude-hud panel list"
priority = 50

[matches]
required_substrings = ["panel\tstate","tokens\tvisible"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
