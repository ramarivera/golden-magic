id = "known.contour-terminal.profiles"
name = "Contour terminal profile list"
priority = 50

[matches]
required_substrings = ["profile\tfont","default\tmonospace"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
