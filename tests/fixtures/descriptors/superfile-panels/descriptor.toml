id = "known.superfile.panels"
name = "superfile panel list"
priority = 50

[matches]
required_substrings = ["panel\tpath","left\t~/Projects"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
