id = "known.musicn.tracks"
name = "musicn track list"
priority = 50

[matches]
required_substrings = ["track\tstatus","intro\tqueued"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
