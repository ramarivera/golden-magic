id = "known.semtools.indexes"
name = "semtools index list"
priority = 50

[matches]
required_substrings = ["index\tstatus","docs\tready"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
