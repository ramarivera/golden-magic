id = "known.dip.services"
name = "dip service list"
priority = 50

[matches]
required_substrings = ["service\tstatus","web\trunning"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
