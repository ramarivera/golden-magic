id = "known.leaf.nodes"
name = "leaf node list"
priority = 50

[matches]
required_substrings = ["node\tstatus","local\tactive"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
