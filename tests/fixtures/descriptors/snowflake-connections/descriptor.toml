id = "known.snowflake.connections"
name = "snowflake connection list"
priority = 50

[matches]
required_substrings = ["connection\tprotocol","prod\tssh"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
