id = "known.dive.layers"
name = "dive image layer list"
priority = 50

[matches]
required_substrings = ["layer\tsize","base\t42MB"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
