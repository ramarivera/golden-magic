id = "known.wslu.commands"
name = "wslu command list"
priority = 50

[matches]
required_substrings = ["command\tstatus","wslview\tready"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
