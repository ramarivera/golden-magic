id = "known.macmon.metrics"
name = "macmon metric list"
priority = 50

[matches]
required_substrings = ["metric\tvalue","cpu\t12"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
