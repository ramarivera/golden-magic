id = "known.terminal-notifier.notifications"
name = "terminal-notifier notification list"
priority = 50

[matches]
required_substrings = ["title\tstate","Build\tshown"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
