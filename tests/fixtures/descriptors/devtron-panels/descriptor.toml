id = "known.devtron.panels"
name = "devtron panel list"
priority = 50

[matches]
required_substrings = ["panel\tstatus","events\tvisible"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
