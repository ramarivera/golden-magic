id = "known.chrome-extension-cli.builds"
name = "chrome-extension-cli build list"
priority = 50

[matches]
required_substrings = ["target\tstatus","chrome\tbuilt"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
