id = "known.concurrently.commands"
name = "concurrently command list"
priority = 50

[matches]
required_substrings = ["command\tstate","web\trunning"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
