id = "known.tracy.panels"
name = "tracy panel list"
priority = 50

[matches]
required_substrings = ["panel\tstatus","request\tvisible"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
