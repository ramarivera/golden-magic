id = "known.pm2.processes"
name = "pm2 process list"
priority = 50

[matches]
required_substrings = ["process\tstatus","api\tonline"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
