id = "known.terminal.tabs"
name = "terminal tab list"
priority = 50

[matches]
required_substrings = ["tab\tstatus","shell\topen"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
