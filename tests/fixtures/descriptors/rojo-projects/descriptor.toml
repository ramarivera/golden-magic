id = "known.rojo.projects"
name = "rojo project list"
priority = 50

[matches]
required_substrings = ["project\tstatus","default\tready"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
