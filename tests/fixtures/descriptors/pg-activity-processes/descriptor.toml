id = "known.pg-activity.processes"
name = "pg_activity process list"
priority = 50

[matches]
required_substrings = ["process\tstate","select\tactive"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
