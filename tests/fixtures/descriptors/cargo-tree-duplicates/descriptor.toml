id = "known.cargo.tree-duplicates"
name = "cargo tree duplicate package list"
priority = 50

[matches]
required_substrings = ["package", "version", "source"]

[parser]
only_rules = ["detect.delimited.pipes"]
