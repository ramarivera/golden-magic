id = "known.hardening.checks"
name = "hardening check list"
priority = 50

[matches]
required_substrings = ["check\tstatus","ssh\tpass"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
