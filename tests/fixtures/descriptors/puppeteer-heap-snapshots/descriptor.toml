id = "known.puppeteer.heap.snapshots"
name = "puppeteer-heap-snapshot snapshot list"
priority = 50

[matches]
required_substrings = ["snapshot\tstatus","baseline\tsaved"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
