id = "known.wacli.workflows"
name = "wacli workflow list"
priority = 50

[matches]
required_substrings = ["workflow\tstatus","deploy\tready"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
