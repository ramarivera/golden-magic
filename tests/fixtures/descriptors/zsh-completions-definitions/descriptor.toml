id = "known.zsh-completions.definitions"
name = "zsh-completions definition list"
priority = 50

[matches]
required_substrings = ["command\tstate","docker\tloaded"]

[parser]
backend = "executable-json"
executable = "./parser-plugin.py"
